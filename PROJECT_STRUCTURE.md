# Responsive Recipe Finder Structure

- app/: Next.js app router pages and layout
- components/: Reusable UI components
- hooks/: Custom React hooks for data fetching

Run `npm install` then `npm run dev` to start.
